A hairline, nothing more. Use Fleuron instead when the rule is separating the wordmark from a product name.

```jsx
<Rule orientation="vertical" length="120px" />
```

Keep it at 1px. A 2px rule reads as a border and fights the ornate frame.
