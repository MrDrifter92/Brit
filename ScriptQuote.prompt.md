The only handwriting on the brand. One per surface, always last, always inside the coloured footer band.

```jsx
<ScriptQuote lines={['Breathe in beauty.', 'Sip in the sunset.']} color="var(--paper-100)" />
```

Two short lines maximum, sentence case with full stops. Never use the script face for a heading, a button, or anything a customer has to read quickly.
